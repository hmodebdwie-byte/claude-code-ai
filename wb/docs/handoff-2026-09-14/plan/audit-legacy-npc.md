######## legacy-npc
ARCH: Two NPC engines share one process (npc-worker, since NPC_WORKER_ENABLED=1 in /home/claude/wb/env/backend.env; the engine process only runs them when NPC_SIM_IN_ENGINE=1, see apps/engine/src/index.ts:270-354). NpcWorkerHostService (apps/npc-worker/src/npc-worker-host.service.ts:83-128) builds ONE legacy NpcSimulatorService (apps/engine/src/npc/npc-simulator.service.ts, 16.8k lines) plus the OrganicNpcEngine bundle (apps/engine/src/npc-organic/create-organic-engine.ts) and wraps both in NpcEngineSwitch (apps/engine/src/npc-organic/npc-engine-switch.ts) which routes START/STOP to whichever engine `npc_engine_mode` selects and switches live on a settings push. Admin START/STOP (apps/api/src/admin/npc/admin-npc.service.ts:1178-1236) only writes Redis `ticktrade:npc:simulator:active` ('1'/'0') and publishes `npc.commands`; NpcWorkerCommandListener (apps/npc-worker/src/npc-worker.command.listener.ts:553-630) polls that key every 2 s and calls switch.start()/stop(); a MISSING key means ON (packages/shared/src/events/npc-simulator.redis.ts:70-73). Engine mode + organic settings are one JSON document written by AdminNpcOrganicService.persist (apps/api/src/admin/npc/admin-npc-organic.service.ts:232-291) to system_settings (`npc_organic_settings`, `npc_engine_mode`), mirrored to Redis (`ticktrade:npc:organic:settings`, `ticktrade:npc:engine_mode`) and pushed on `engine:events:npc.organic.settings`; OrganicSettingsSource (apps/engine/src/npc-organic/organic-settings.source.ts) applies the push and polls Redis→DB every 20 s, while the API hot path (packages/shared/src/npc-organic/engine-mode.redis.ts) reads Redis only and defaults to 'organic'. All legacy settings are flat system_settings rows written by three admin services (admin-npc-rotation/volume-shape/pool-asymmetry.service.ts) plus `npc_behavior_preset`/`npc_aggressiveness_multiplier` written by admin.service.ts:2996-3071 (/admin/battle/settings/npc-behavior); the legacy simulator reloads all ~100 keys in refreshConfig (npc-simulator.service.ts:16358-16801) at most every 60 s (NPC_CONFIG_REFRESH_MS, :780) and on every new battle (:11750). In LEGACY mode three producers place NPC trades concurrently: simulateTick (:11689+, phases/layers), the liquidity heartbeat (:8848-9024), and NpcIntelligenceRunner (apps/npc-worker/src/npc-intelligence-runner.ts, packages/npc-intelligence) plus reactive bursts in the worker listener (:637-699); only the first honours rotation/volume-shape/asymmetry fully. Every NPC trade commits through placeNpcTrade (:15350-15815) / forceNpcTradeNoCap (:14760+) / executeOrganicNpcTrade (:15823-15921): NPC balance decrement (with auto top-up), npc_trades row (or Redis buffer), BattlePotCache deltas, leaderboard zset, then NpcTradePublishCoalescer (apps/engine/src/npc/npc-trade-publish.coalescer.ts) publishes `battle.snapshot` + `npc.trade` at most once per second per battle, and onTradeExecuted publishes `engine:events:trade.executed` (userId 0) which the engine turns into a price tick with tradeAttribution 'npc' (apps/engine/src/redis/redis.subscriber.ts:118-183 → apps/engine/src/events/trade-executed.handler.ts:275-299). The API RedisSubscriber (apps/api/src/redis/redis.subscriber.ts) forwards price ticks (chart/candles), battleSnapshot (pots + trader_count incl. NPC zsets) and topTradersUpdate (NPC rows) to the PWA; it deliberately does NOT emit any per-NPC-trade WS event (:3136-3157). Admin UI: npcSimulator.jsx hosts six tabs (NPC Traders, Organic Engine = NpcOrganicEngine.jsx driven by GET /admin/npc/organic/schema, Rotation & Limits, Volume Distribution, Pool Balance, NPC Behavior).
KEY FILES: backend/apps/engine/src/npc/npc-simulator.service.ts (Legacy simulator (16.8k lines): refreshConfig :16358-16801 loads every legacy key; pickRotatedNpcId :6792-6849; volume plan :6346-6391/:6675-6759; asymmetry :6506-6565/:8472-8700; placeNpcTrade :15350-15815; executeOrganicNpcTrade :15823-15921; behaviour presets :15923-16311; simulateTick :11689+; liquidity heartbeat :8848-9024)
  backend/apps/npc-worker/src/npc-worker-host.service.ts (Process wiring: builds legacy sim + organic bundle + NpcEngineSwitch + command listener + intelligence runner)
  backend/apps/npc-worker/src/npc-worker.command.listener.ts (START/STOP poll+command (:553-630), reactive bursts (:637-699), battle/trade event routing per mode)
  backend/apps/npc-worker/src/npc-intelligence-runner.ts (Third legacy producer (800 ms tick, uniform NPC pick :219-240))
  backend/apps/engine/src/npc-organic/npc-engine-switch.ts (Mode-aware start/stop routing)
  backend/apps/engine/src/npc-organic/organic-settings.source.ts (Engine-side reader of organic doc + mode (push, Redis poll, DB fallback))
  backend/apps/engine/src/npc-organic/organic-npc-engine.ts (Organic engine round loop; rotation hook :534-536; final injection :619-740)
  backend/apps/engine/src/npc-organic/create-organic-engine.ts (Organic deps; partial rotation integration :70-147)
  backend/packages/shared/src/npc-organic/settings.ts (ORGANIC_FIELD_SPECS schema (54 fields, groups, applies tags), validate/coerce)
  backend/packages/shared/src/npc-organic/engine-mode.redis.ts (API hot-path mode reader (Redis only, default organic))
  backend/packages/shared/src/events/npc-simulator.redis.ts (NPC ON/OFF Redis key + isNpcSimulatorDesiredActive (missing => ON))
  backend/packages/shared/src/npc/npc-rotation.ts (Rotation parsing, window check, selectNpc strategies, min-distinct guard)
  backend/packages/shared/src/redis/npc-rotation.redis.ts (Lua eligibility/commit (caps, cooldown, hard floor, no-repeat))
  backend/packages/shared/src/npc/npc-volume-shape.ts (A7 volume plan math + parse/validate (targetAmountNudgeMax parsed :413 but unused))
  backend/packages/shared/src/npc/npc-pool-asymmetry.ts (A7b resolvers: convergePressure, resolvePoolAsymmetry, drawRoundLeadTarget, leadSteerStrength)
  backend/apps/api/src/admin/npc/admin-npc.service.ts (Trader CRUD/CSV, start/stop/status (:1178-1283))
  backend/apps/api/src/admin/npc/admin-npc-rotation.service.ts (PUT /admin/npc/rotation-settings -> npc_rotation_* rows)
  backend/apps/api/src/admin/npc/admin-npc-volume-shape.service.ts (PUT /admin/npc/volume-shape-settings -> npc_volume_* rows)
  backend/apps/api/src/admin/npc/admin-npc-pool-asymmetry.service.ts (PUT /admin/npc/asymmetry-settings -> npc_asymmetry_* rows)
  backend/apps/api/src/admin/admin.service.ts (npc-behavior pace/aggression rows :2975-3071 (normal stored as ''))
  backend/apps/api/src/admin/npc/admin-npc-organic.service.ts (Organic doc CRUD, mode switch, live/rounds endpoints)
  backend/apps/api/src/redis/redis.subscriber.ts (Redis->WS fan-out; handleNpcTrade drops the npcTrade WS event :3136-3157; handlePriceTick :768-888)
  backend/apps/engine/src/battle/battle-snapshot.publisher.ts (battleSnapshot payload incl. trader_count from NPC zsets :279-290)
  backend/apps/api/src/trades/trades.service.ts (API gates on engine mode (:1457, :2871-2880) and NPC active flag (:1654, :3203, :3446, :4926))
  admin/src/Pages/AdminDashboard/npcSimulator.jsx (Legacy tabs UI (rotation :1875-2129, volume :2131-2468, pool :2470-2748, behavior :2750-2856), dead recent_trades list :1308-1321)
  admin/src/Pages/AdminDashboard/NpcOrganicEngine.jsx (Organic tab (status bar, schema-driven form, live, audits))
  admin/src/Pages/AdminDashboard/npcOrganicApi.js (Organic REST client, getPath/withPath, field error mapping)
  admin/src/Pages/AdminDashboard/NpcSimulator.css (rotation-* card system (:1582-1785) and organic-* styles (:1786-1844))
  admin/src/Pages/AdminDashboard/BattleSettings.jsx (Design reference: detail rows + pencil modal + info boxes)
  admin/src/Pages/AdminDashboard/BattleSettings.css (Design reference tokens (.battle-details-card, .battle-detail-item, .edit-icon-btn, .rotation-info-box))
  admin/src/Pages/AdminDashboard/MarketSensitivity.jsx (Design reference: steppers, immediate-save toggles, live preview card, trade log table)
  admin/src/Pages/AdminDashboard/AdminDashboard.css (Shared tokens: .section-card/.section-title (:528-558), .form-input/.form-select (:1256-1318), .admin-table (:1061-1108), .modal-overlay/.modal-card (:1576-1713))
  frontend/src/utils/useWebSocket.js (PWA socket handlers (no npcTrade event; trade feed = trade:active:stream = user's own stakes))

## INV-1 [CONFIRMED/low] Inventory: NPC ON/OFF flag (START/STOP)
SYMPTOM: Admin shows SIMULATOR RUNNING although nobody pressed START
EVIDENCE: Write: admin-npc.service.ts:1178-1191 `redisPublisher.set(NPC_SIMULATOR_ACTIVE_REDIS_KEY, NPC_SIMULATOR_ACTIVE_ON)` + publish npc.commands; stop :1214-1223 writes '0'. Key = 'ticktrade:npc:simulator:active' (npc-simulator.redis.ts:5). Read: npc-worker.command.listener.ts:557-578 (poll 2 s) and :607-630 (pub/sub) -> NpcEngineSwitch.start/stop (npc-engine-switch.ts:32-48); API trades.service.ts:1654,3203,3446,4926 (hedge/dominance + settlement projections). Status endpoint admin-npc.service.ts:1238-1283 returns desiredActive + worker heartbeats 'ticktrade:npc:worker:active'/'leader'. Observed now: Redis key EMPTY, worker active=1.
MECH: WIRED for both engines. Not persisted in DB; missing key means ON (see BUG-1).
FIX: See BUG-1.

## INV-2 [CONFIRMED/low] Inventory: engine mode (organic|legacy)
SYMPTOM: 
EVIDENCE: Write: PUT /admin/npc/organic/mode -> admin-npc-organic.service.ts:129-137 -> persist :232-291 writes system_settings npc_engine_mode + npc_organic_settings, Redis 'ticktrade:npc:engine_mode' + 'ticktrade:npc:organic:settings', publishes 'engine:events:npc.organic.settings'. Read (engine/worker): organic-settings.source.ts:98-118 (Redis, DB fallback) -> NpcEngineSwitch.applyMode :50-62; worker listener routes events by isOrganic() (:224-231,:493,:501,:527-536). Read (API): engine-mode.redis.ts:16-34 Redis only, default 'organic'. Observed: DB npc_engine_mode=organic, Redis key EMPTY.
MECH: WIRED. API side has no DB fallback (see BUG-2).
FIX: See BUG-2.

## INV-3 [CONFIRMED/low] Inventory: NPC Behavior tab (pace + aggression) — legacy only
SYMPTOM: 
EVIDENCE: Write: PUT /admin/battle/settings/npc-behavior (admin.controller.ts:1127-1148, admin.service.ts:2996-3071): `npc_behavior_preset` = 'calm'|'intense'|'' (normal stored as empty string :3027) and `npc_aggressiveness_multiplier`. Read: refreshConfig sim :16489-16517; pace -> applyNpcBehaviorPreset :15973-16021 (tick interval, battle-flow gaps/sizes, warm-up, quiet periods, participation base, post-real-trade cooldown), applyNpcPaceOutcomeStrategy :16261-16311, ~20 getNpcPace* multipliers :16024-16245, worker intelligence/reactive tier params via getNpcPaceTier (:6875; npc-worker.command.listener.ts:74-107,159-161). Aggression: participation^0.28 :13557-13560; per-NPC aggressiveness×mult :14109; stake ladder ×m^0.72 clamped [0.3,3] :14739/:14752/:16251-16255; structural caps clamp [0.35,3] :16126-16128; counter caps :16765-16785. No timing code reads it.
MECH: WIRED (legacy). Organic engine never reads these keys (grep: no reference in apps/engine/src/npc-organic or packages/shared/src/npc-organic).
FIX: Keep; fix UI default/copy (BUG-9) and the force-win side effect (BUG-8).

## INV-4 [CONFIRMED/low] Inventory: Rotation & Limits (12 keys npc_rotation_*)
SYMPTOM: 
EVIDENCE: Write: PUT /admin/npc/rotation-settings (admin-npc-rotation.service.ts:57-111), DTO npc-rotation-settings.dto.ts. Read legacy: refreshConfig :16433-16444 -> parseRotationSettings :16481 (npc-rotation.ts:103-160) -> pickRotatedNpcId :6792-6849: enabled :6778; window_from/to :6802 (isWithinAllowedWindow npc-rotation.ts:172-186, server-local getHours); max_per_day/week/month, cooldown, hard_floor, no_repeat via Lua checkNpcRotationEligibility (npc-rotation.redis.ts:80-113,160-223); min_distinct :6828 (applyMinDistinctGuard, clamped to 80% of pool); strategy :6830 (selectNpc npc-rotation.ts:271-327); commit per trade :15729-15737 (drawCooldownMs); defense paths stamp last only :14957,:15063; reconciler :6901-6925. Read organic: create-organic-engine.ts:70-147 loadRotation (20 s TTL): isNpcEligible uses only the Lua caps/cooldown/hard-floor/no-repeat; onNpcParticipated commits per trade + npc_traders.lastParticipatedAt.
MECH: PARTIALLY WIRED: window/strategy/min_distinct legacy-only; caps/cooldown both; every producer except simulateTick/heartbeat bypasses it; starvation falls back silently (BUG-3/BUG-4). DB currently: enabled=false, defaults.
FIX: See BUG-3/BUG-4.

## INV-5 [CONFIRMED/low] Inventory: Volume Distribution (13 keys npc_volume_*) — legacy only
SYMPTOM: Leaderboard shape does not change visibly when settings change (weights only, and only on simulateTick/heartbeat picks)
EVIDENCE: Write: PUT /admin/npc/volume-shape-settings (admin-npc-volume-shape.service.ts:109-180). Read: refreshConfig :16445-16457 -> parseVolumeShapeSettings :16485. enabled/alpha_min/alpha_max/slot_jitter/planned_trader_count/planned_use_percent/planned_trader_pct/leader_no_repeat_rounds -> buildVolumePlan (shared npc-volume-shape.ts:548-623) called from getOrBuildVolumePlan :6367 once per battle (:6359, reset :11808-11815); targets only change selection weight (volumeWeightFor :6675-6705, capped 2x by volumeDeficitWeight) in pickWeightedByVolumeTarget :6716-6759 / pickRotatedNpcId :6825; plan_only_picks :6737-6743; respect_top_real_user :6691-6699 (leader only, topRealUserStake :14568); quiet_market_floor/ceiling -> computeActivityScale :14660-14673 -> npcActivityScale -> getNpcTotalVolumeCeiling :3723 (reference rolled per battle :14679-14689, in-memory only). Volume is accumulated only in placeNpcTrade :15811 (not forceNpcTradeNoCap/executeOrganicNpcTrade). `npc_volume_target_amount_nudge_max`: parsed (shared :413-418), validated, persisted, but zero references in the engine (grep targetAmountNudge in npc-simulator.service.ts = 0) and no admin field.
MECH: WIRED (legacy selection-weighting only) except target_amount_nudge_max = DEAD. Not read by organic engine (organic uses tiers instead). DB currently has no npc_volume_* rows (defaults, disabled).
FIX: Remove or implement target_amount_nudge_max; see BUG-3 for producers that bypass the plan.

## INV-6 [CONFIRMED/low] Inventory: Pool Balance (13 keys npc_asymmetry_*) — legacy only
SYMPTOM: 
EVIDENCE: Write: PUT /admin/npc/asymmetry-settings (admin-npc-pool-asymmetry.service.ts:109-183). Read: refreshConfig :16460-16472 -> parsePoolAsymmetrySettings :16486; per-round draw getOrDrawAsymmetryPlan :6506-6541 (round_lead_frac_min/max via drawRoundLeadTarget shared :619-635, reset :11819-11822); resolveAsymmetry :6551-6565 (convergePressure/converge_start_frac shared :496-587). Consumers: counterbalance_parity_min/max :8495-8499; weaker_side_bias :8592; npc_only_mean_revert_bias :8580; max_skew_frac :5117,:8624,:8653,:8692; max_pot_lead_ratio :5907; extreme_skew_seed_ratio :3844; balance_band_low/high -> liquidity heartbeat :8974 and phase-1 liquidity :13504 (quiet-period-liquidity.engine.ts:60-61); lead steer asymmetryLeadTargetFor :8607-8627 used at :3494,:4297,:8976,:13506 and lead_steer branch quiet-period-liquidity.engine.ts:90-93; size multiplier :13665.
MECH: WIRED (legacy). Organic engine has its own asymmetry group. DB currently has no npc_asymmetry_* rows (defaults, disabled).
FIX: See BUG-11 for battle-id keying inconsistency.

## INV-7 [CONFIRMED/low] Inventory: liquidity_* keys are read by the legacy simulator but have no admin UI
SYMPTOM: Changing pace/tick settings barely changes visible NPC tape density
EVIDENCE: refreshConfig :16722-16745 reads liquidity_inactivity_seconds, liquidity_size_min/max, liquidity_mm_size_min/max, liquidity_heartbeat_interval_ms; the heartbeat (:8848-9024) is the layer that places most legacy NPC trades (comment :8603 '~90% of all NPC trades'). No admin page writes these keys (grep in admin/src: none); DB has no rows.
MECH: Hidden but active knobs; the pace preset's 'tick rate' copy does not cover this producer.
FIX: Expose liquidity_* on the NPC Behavior tab or fold heartbeat cadence into the pace preset.

## INV-8 [CONFIRMED/low] Inventory: organic settings document (54 fields) — all wired, two 'applies' tags inaccurate
SYMPTOM: 
EVIDENCE: Schema packages/shared/src/npc-organic/settings.ts:227-310. roundStart.* -> planInitialWave/isPeakTime (organic-npc-engine.ts:357-359, frozen :378); momentum.* -> arrivalsPerMinute/drawQuietPeriodMs/maxDistinctNpcsPerRound/maxSimVolumePerRound (:474-484,:496,:516-517, boost :258-260); tiers.* -> assignTiers/pickTier/drawStake (organic-plan.ts:137-203); asymmetry.* -> drawLeadTarget/chooseSide (:372, :520-532, frozen); defenses.* -> chooseSide steering (organic-plan.ts:256-263), refreshProjection :587-597, runFinalInjection :631-740; audit.keepRounds :447. `defenses.finalInjectionLeadMs` is tagged 'live' but the final timer is armed once at session open (:619-629) so a mid-round change only affects arrival cut-off (:473,:515). `finalInjectionMaxSlices` help says 'whale NPCs' but candidates are whale+mid+retail (:671). `engine.mode` field inside the doc is redundant with PUT /mode.
MECH: Correct wiring; UI copy mismatch only.
FIX: Tag finalInjectionLeadMs 'next_round' or re-arm the timer on change; fix slice help text; hide engine.mode field from the form (status bar already switches).

## BUG-1 [CONFIRMED/critical] NPC OFF is not durable: Redis-only flag, and a missing key means ON
SYMPTOM: NPC OFF flag ignored / NPCs come back by themselves
EVIDENCE: packages/shared/src/events/npc-simulator.redis.ts:70-73 `if (raw == null || raw === '') return true;`. Admin stop writes only Redis (admin-npc.service.ts:1214-1218). Worker poll starts the controller whenever `want` is true (npc-worker.command.listener.ts:564-573). Engine listener even re-persists '1' on boot when the key is missing (npc.command.listener.ts:125-140). Observed: `redis-cli GET ticktrade:npc:simulator:active` = '' while `ticktrade:npc:worker:active` = '1' and organic status active:true (no START recorded).
MECH: Any Redis restart/flush/eviction after an admin STOP silently re-enables NPC trading (which spends npc_traders.balance with auto top-up, and in legacy mode force-wins pots against real users). The admin status derives `active` from the same default so it reports RUNNING.
FIX: Persist desired state in system_settings (e.g. `npc_simulator_active`) from AdminNpcService.start/stopSimulator; make isNpcSimulatorDesiredActive default OFF when the key is missing and have the worker's syncDesiredStateFromRedis fall back to the DB row (like OrganicSettingsSource.refresh); mirror back to Redis on boot; show 'source: redis|db|default' in getSimulatorStatus.

## BUG-2 [CONFIRMED/high] API engine-mode reader has no DB fallback and defaults to organic; engine falls back to DB — the two can disagree
SYMPTOM: 
EVIDENCE: packages/shared/src/npc-organic/engine-mode.redis.ts:16-34 reads only NPC_ENGINE_MODE_REDIS_KEY and returns 'organic' when null (parseNpcEngineMode :515-517) or on error. Engine/worker: organic-settings.source.ts:100-111 falls back to system_settings. The Redis key is written only by AdminNpcOrganicService.persist (:268-270); nobody re-mirrors it. Observed: Redis `ticktrade:npc:engine_mode` = '' right now. API uses the mode at trades.service.ts:1457-1459 and :2871-2880 (`awaitEndgameDominance = !isRpcBot && !organicMode && ...`), and for ROUND_LOCKED handling.
MECH: With DB=legacy and the Redis key gone, the worker runs the legacy simulator while every API worker believes 'organic': the API skips the endgame dominance await (real user order returns before the legacy counter lands) and applies organic-only gates. Money-affecting in legacy mode.
FIX: In OrganicSettingsSource.refresh, when Redis was empty and DB was used, write the mode/doc back to Redis; and make readNpcEngineModeFromRedis accept a DB getter fallback (or have the API subscribe to NPC_ORGANIC_SETTINGS_CHANNEL and prime the key on boot).

## BUG-3 [CONFIRMED/high] Rotation limits count trades, not sessions, are advisory under starvation, and are bypassed by 3 of the 5 NPC producers
SYMPTOM: Rotation settings have no visible effect; same NPCs trade constantly
EVIDENCE: (a) commit per trade: npc-simulator.service.ts:15729-15737 `if (this.rotationSettings.enabled) { const cooldownMs = drawCooldownMs(...); void commitNpcRotationParticipation(...)}` and create-organic-engine.ts:135-141 — so max_per_day=12 = 12 trades, and ONE trade puts the NPC in a 2-6 h cooldown (defaults). (b) starvation fallback: :6798-6808 outside window -> `return fallback()`; :6831-6839 picked==null -> `console.warn('rotation starvation...'); return fallback()` (uniform/volume-weighted). (c) bypass: NpcIntelligenceRunner :219-240 `npcIds[Math.floor(Math.random()*npcIds.length)]` via getActiveNpcIdsForWorker (:7560, isActive only); reactive burst npc-worker.command.listener.ts:648-693 same; forceNpcTradeNoCap defense trades only stampNpcRotationLastOnly (:14957,:15063); open seed pickSeedNpcForSide :9038-9054 (by design). (d) organic: create-organic-engine.ts:125-134 checks only the Lua caps; window_from/to, strategy, min_distinct never applied; organic-npc-engine.ts:534-536 `if (this.deps.isNpcEligible && !(await ...)) return;` drops the arrival instead of re-picking. legacy ignores npc_traders.rotationPaused (getActiveNpcIds :7024-7027 filters isActive only; organic roster :105 filters it).
MECH: With a 20-NPC pool (DB: 20 active) and defaults, all NPCs enter cooldown within the first minute, after which every pick silently ignores the limits; with a large pool the limits shape only simulateTick/heartbeat picks. The admin copy ('sessions', 'Applies across trading feed, leaderboard...') is not what runs.
FIX: Define a participation session as (npcId, battleId): commit once per round per NPC (first landed trade), not per trade. Centralise NPC selection in one `pickEligibleNpc(battleId, side, {bypassCaps})` used by simulateTick, heartbeat, intelligence runner, reactive bursts, defense paths and the organic engine (filter the tier bucket before pickNpc). Replace the silent fallback with an explicit policy setting ('starve = pause NPC' vs 'relax caps') and mirror starvation state to Redis/admin status. Apply rotationPaused in getActiveNpcIds.

## BUG-4 [CONFIRMED/medium] Rotation strategy 'least_recently_used' is effectively random; 'round_robin' is process-local and non-cyclic
SYMPTOM: 
EVIDENCE: npc-simulator.service.ts:6823 builds candidates with `lastParticipatedAtMs: null` for every NPC; selectNpc LRU (npc-rotation.ts:301-317) compares `?? -Infinity` so all tie and a random tie-break wins. round_robin uses module-level `roundRobinCursor` (npc-rotation.ts:259-323) over a per-pick re-sorted, changing candidate subset; the Redis cursor helper nextRoundRobinCursor (npc-rotation.redis.ts:305-318) is never called.
MECH: Strategy radio in the admin has no behavioural difference between weighted_random/LRU; RR resets on restart and skips/repeats as the candidate set changes.
FIX: Populate lastParticipatedAtMs from the Redis `last` keys (already fetched by the Lua script — return it) or npc_traders.lastParticipatedAt; use nextRoundRobinCursor over the full roster ordering, then skip ineligible.

## BUG-5 [CONFIRMED/high] Saving any NPC Behavior pace forces force-win ON and overrides npc_force_win_enable
SYMPTOM: 
EVIDENCE: admin.service.ts:3026-3039 always upserts `npc_behavior_preset` ('' for normal). refreshConfig reads `npc_force_win_enable` at :16623-16624, then :16788 calls applyNpcPaceOutcomeStrategy which, whenever the preset row exists (:16262-16265 `if (row == null) return;`), sets `this.forceNpcWinEnable = true;` (:16285) and rewrites forceWinFavorRealSide/Majority/MixedHeavy from other keys/env (:16287-16310). Default before any save: FORCE_NPC_WIN_ENABLE env ('1' default, :348).
MECH: After the admin saves the Behavior tab once (even 'Normal'), the advanced key `npc_force_win_enable=false` is silently ignored forever; isCounterWinMode (:3146-3148) then depends only on favor-real-side. Money-affecting: force-win/counter-win injects NPC volume to flip pots at close.
FIX: In applyNpcPaceOutcomeStrategy respect an explicit `npc_force_win_enable` row (dbBool) and only default to true when absent; document the coupling in the Behavior tab.

## BUG-6 [CONFIRMED/medium] Legacy mode runs three independent NPC producers with different rule sets; pace/aggression/rotation/volume/asymmetry each apply to a different subset
SYMPTOM: Settings changes barely change what players see
EVIDENCE: Producers in the worker when legacy is active: (1) simulateTick chain :11689+ -> runPhase1WithLayers (liquidity layer :13497-13540 respects asymmetry+rotation via pickNpcForSide; activity layer :13552-13700 uses pace profile, aggression, asymmetry size multiplier, per-NPC aggressiveness via decidePhase1Trade :14101-14116 but iterates the shuffled roster directly, not pickNpcForSide); (2) liquidity heartbeat :8848-9024 (liquidity_* keys, pickNpcForSide); (3) NpcIntelligenceRunner (npc-intelligence-runner.ts:87-149, 800 ms tick, gated only by isActive() and pace tier; sizes via placeNpcIntelligenceDefenseTrade :7389-7480 -> forceNpcTradeNoCap; uniform NPC pick) and reactive bursts (npc-worker.command.listener.ts:372-409 with hard-coded tier params :74-107). npcAggressivenessMultiplier is read only in (1) (:13557,:14109,:14739,:16128,:16765); rotation/volume weighting only in pickRandomNpcIdForSide (:6761-6782) used by (1-liquidity) and (2).
MECH: Admin copy ('Calm: slower tick rate, longer quiet periods', 'Aggression ... react faster') describes (1) only; the tape the player sees is dominated by (2)/(3). Any 'make legacy correct' effort must first decide which producer owns which behaviour.
FIX: Route (2) and (3) through the same selection gate and the same per-tick budget as (1); make aggression scale reactive/intelligence chunk sizes too (or drop the claim from the UI); expose liquidity_* keys.

## BUG-7 [CONFIRMED/medium] Legacy NPC fills never reach the PWA as trades; only aggregates do (pots, chart, leaderboard, trader count)
SYMPTOM: Owner expects NPC activity to be 'linked to the frontend'
EVIDENCE: apps/api/src/redis/redis.subscriber.ts:3136-3157 handleNpcTrade: 'The standalone `npcTrade` WS event was removed... individual NPC fills aren't rendered anywhere' — only celebration notifications. Engine side coalesces npc.trade to one payload per battle per second and overwrites pending fills (npc-trade-publish.coalescer.ts:46-47 `this.npcTradePending.set(battleId, payload)`; ENGINE_NPC_PUBLISH_COALESCE_MS default 1000). PWA socket events (frontend/src/utils/useWebSocket.js) contain no npcTrade; trade feed store is fed by trade:active:stream (user's own stakes, :1040-1045). What does arrive: pots via `battleSnapshot` (battle-snapshot.publisher.ts:223-261, coalesced :2471-2505 API) -> useVisiblePots.js; trader_count = real user ids + NPC zset cardinalities (:279-290) -> matchStore.js:57; chart price+volume via trade.executed(userId 0) -> recalcPriceFromTrade (engine redis.subscriber.ts:182) -> priceTick tradeAttribution 'npc' + volumeScale (trade-executed.handler.ts:275-299) -> API handlePriceTick :842-848 (candles) + priceUpdate; leaderboard via addNpcStake -> topTradersUpdate (Leaderboard.jsx:191 `npc-${player.npc_id}`). Organic 'Live round' telemetry exists only in admin (Redis ticktrade:npc:organic:live).
MECH: 'Linked to the PWA' today means aggregates only; there is no per-trade NPC tape, and the 1 s coalescer would drop fills even if one were added.
FIX: If a public tape is wanted: emit a batched `tradeTape` WS event from handleNpcTrade (keep username/avatar via a cached npc lookup), make the coalescer accumulate an array instead of overwriting, and reuse the same event for real trades; otherwise document that NPC activity surfaces only via pots/chart/leaderboard/trader count.

## BUG-8 [CONFIRMED/medium] Admin NPC Behavior tab: wrong default pace, misleading aggression copy, unbounded input
SYMPTOM: 
EVIDENCE: npcSimulator.jsx:46-49 `DEFAULT_NPC_CONTROL_FORM = { pace: 'calm', aggression: '1' }` used by 'Reset to defaults' (:2841) and modal open (:1063); backend default is 'normal' (admin.service.ts:2985-2987) and the radio label reads 'Normal (default)' (:2773). Copy :2831-2832 'Values above 1 make NPCs trade larger and react faster' — no timing code reads the multiplier (INV-3). Input min 0.1 no max (:2818-2826) while the engine clamps to [0.3,3] (:16254) / [0.35,3] (:16128).
MECH: Reset silently switches the platform to calm; admins cannot tell that values >3 are ignored.
FIX: Default 'normal'; max=3 step 0.1 with clamp note; rewrite copy to 'scales stake sizes and defense caps (0.3-3x), does not change timing'.

## BUG-9 [CONFIRMED/low] Admin NPC Traders tab shows a dead 'recent trades' list and a status derived from the default-ON flag / per-process memory
SYMPTOM: 
EVIDENCE: npcSimulator.jsx:596 `setRecentTrades(res.recent_trades || [])` and :1308-1321 render; GET /admin/npc/simulator/status (admin-npc.service.ts:1238-1283) never returns recent_trades. `engineReportedActive` comes from NpcStatusService (apps/api/src/redis/npc-status.service.ts, in-memory per API process). `active: desiredActive` = true when the Redis key is missing.
MECH: Empty list forever; status can differ between API workers and reports RUNNING without a START.
FIX: Drop the list or feed it from the organic live key / a small Redis ring of last NPC trades; derive engineReportedActive from the worker heartbeat keys only.

## BUG-10 [CONFIRMED/medium] Organic engine drops rotation-ineligible arrivals and ignores window/strategy/min-distinct; UI claims 'Rotation & Limits still apply in organic mode'
SYMPTOM: Fewer NPC trades than the momentum settings promise when rotation is on
EVIDENCE: organic-npc-engine.ts:534-536 `const npcId = this.pickNpc(...); if (npcId == null) return; if (this.deps.isNpcEligible && !(await this.deps.isNpcEligible(npcId))) return;` (arrival lost); create-organic-engine.ts:125-134 only calls checkNpcRotationEligibility; NpcOrganicEngine.jsx:215 intro text.
MECH: With rotation enabled the organic arrival rate silently falls below the configured momentum; window/strategy/min-distinct are no-ops in organic mode.
FIX: Filter tier buckets by eligibility (batch Lua check over the bucket, cached a few seconds) before pickNpc; apply the allowed window as an engine-level gate; update the intro copy to list exactly which rotation fields apply.

## BUG-11 [CONFIRMED/low] A7b asymmetry plan keyed inconsistently (stats.battleId vs this.currentBattleId) across a battle transition
SYMPTOM: 
EVIDENCE: resolveAsymmetry :6556 `const planBattleId = stats.battleId || this.currentBattleId;` (comment :6553-6555 explains the double-draw); but asymmetrySizeMultiplierFor :8641-8642 and steerSideTowardAsymmetryTarget :8679-8680 use `this.currentBattleId`; pinBattleTiming advances currentBattleId at battle.started while the new-battle init (:11819-11822) deletes every plan including the new id.
MECH: Between battle.started and the first simulateTick of the new round, side steering and size multiplier read a plan drawn for the new id that is then wiped and redrawn — two different lead sides can be applied within one round's first seconds.
FIX: Pass stats.battleId to both helpers (they already receive stats).

## BUG-12 [CONFIRMED/low] Rotation window uses server-local time; organic peak hours use an explicit UTC offset
SYMPTOM: 
EVIDENCE: npc-rotation.ts:180 `now.getHours()`; organic settings.ts:531-544 use peakTimezoneOffsetMinutes; admin copy 'server-local' (:1902-1904 rotation card, DTO description).
MECH: On a UTC host the 08:00-22:00 window is UTC, not the operator's local time; inconsistent with the organic tab.
FIX: Add npc_rotation_timezone_offset_minutes (or reuse the organic offset) and evaluate the window with it.

## UI-1 [CONFIRMED/low] Design language of BattleSettings.jsx / MarketSensitivity.jsx to reuse for the Organic tab redesign
SYMPTOM: Owner wants the Organic tab to feel like the dev-tool pages
EVIDENCE: BattleSettings.jsx:251-527 + BattleSettings.css: page `.battle-settings-page`; header `.section-card > .section-header > .section-title (18px 800 #fff) / .section-subtitle (13px #666)` (AdminDashboard.css:528-558); section = `.battle-details-card` (bg #090b0e, 1px rgba(255,255,255,.08), radius 20, padding 32, shadow 0 12px 36px) with `.battle-details-header/.battle-details-title` (24px 700); `.battle-details-list` (gap 14) of `.battle-detail-item` rows (bg #0f1217, radius 14, padding 20 28, flex space-between, hover #13171e): `.item-left` [optional `.dot-cyan` 9px glow] + `.item-label` (16px 600 #fff); `.item-right` `.item-value` (15px #8a96a3, formatted with unit, 'Disabled' for 0) + `.edit-icon-btn` (38px, cyan outline rgba(0,214,222,.35), pencil SVG #00d6de) or `.um-status-badge active|pending` for booleans. Every row is followed by `.rotation-info-box` (bg rgba(0,214,222,.05), border-left 3px #00d6de, 12px #bbb, radius 0 4 4 0) holding `.rotation-italic-text` plain-language help; `<strong>` (cyan) marks save semantics ('Clicking the pencil here saves immediately', 'only affects battles that start after you save'). Edit flow: pencil -> `.modal-overlay` (blur) + `.modal-card` 420px (AdminDashboard.css:1576-1713): `.modal-title`, `.modal-label` with the allowed range, single `.modal-input type=number` (min/max/step), `.btn-cancel` + `.btn-save.save-btn` (gradient #3baeff->#6b5bff), Enter saves, toast on success/error, per-field validation message. MarketSensitivity.jsx:298-660: `.section-card` header; ℹ️ formula banner (bg rgba(0,214,222,.08), border rgba(0,214,222,.3), radius 8, 13px, <code> cyan); 2-column grid (gap 20) of `.section-card` panels (padding 20); labels 11px 700 #888 uppercase letter-spacing .06em; stepper control (bg #0a0a0a, border #282828, radius 8, — value +; value 22px 800 monospace); `.form-input` overrides bg #0a0a0a/border #282828 14px 700; each control followed by `.rotation-info-box` stating 'Applies after Save Configuration' vs 'Saves immediately when flipped'; full-width `.btn.btn-primary` whose label appends '(unsaved changes)'; iOS toggle 38x20 (#00e676 on, #333 off, thumb 14px); full-width Live Preview card (gridColumn 1/-1, border rgba(0,214,222,.3), gradient bg) with '• LIVE' green pill (10px 700 #00e676 on rgba(0,230,118,.1)), 3-col grid: test input / 'WITH THE SETTINGS ON THIS PAGE (UNSAVED)' (cyan 20px 800) / 'ENGINE NOW (SAVED, LIVE)' (green) each with a 9px #666 footnote of the parameters used; stat boxes bg #0a0a0a border #282828 radius 8, label 10px uppercase; `.admin-table-wrapper > .admin-table` (AdminDashboard.css:1061-1108) with NPC/REAL pills (amber #f59e0b vs cyan) and change history with `.um-status-badge`. NPC legacy tabs (NpcSimulator.css:1582-1785) add `.rotation-grid` (auto-fit 280px), `.rotation-card` (bg #121820, border #1e293b, radius 8, padding 20, gap 20), `.rotation-card-title` (14px 700 cyan uppercase + 18px SVG icon), `.rotation-label` (12px 600 #aaa uppercase), `.rotation-input-container` (bg #0f141a border #2a3441 radius 6) with prefix/suffix, `.rotation-radio-label(.active)`, `.rotation-footer` (right-aligned `.rotation-reset-link` + pink `.rotation-save-btn` #ff007f), master-switch card with iOS toggle 44x24, PREVIEW card with 🎲 Reshuffle and amber advisory warning cards (borderLeft #ffb300).
MECH: Colour system: bg #0a0a0a/#0f0f0f/#121820, borders #1a1a1a/#282828/#1e293b, accent cyan #00d6de, buy/sell = #00d6de/#ff2d86 in MarketSensitivity but #00e676/#ff5252 in NPC tabs, and the organic tab uses a third palette (#2ecc71/#e74c3c/#ffd700/#5dade2/#b784d6, NpcSimulator.css:1786-1844) — the redesign should adopt the cyan/pink tokens and the info-box-per-field pattern.
FIX: Rebuild NpcOrganicEngine.jsx settings view as `.battle-details-card` sections per ORGANIC_GROUPS with `.battle-detail-item` rows (label, formatted value+unit, applies pill as `.um-status-badge`, pencil -> `.modal-card` editor showing help/min/max/default), keep a sticky footer with 'Save & push to engine (N unsaved)' like MarketSensitivity's primary button, and move Live round into a MarketSensitivity-style Live Preview card (• LIVE pill, stat boxes, pot bar, recent trades `.admin-table`).

## UI-2 [CONFIRMED/low] Structure of the current Organic Engine tab (NpcOrganicEngine.jsx) for the redesign
SYMPTOM: 
EVIDENCE: Root `.rotation-limits-wrapper.organic-wrapper` (:599). EngineStatusBar (:60-110): 3 `.organic-status-cell`s — mode pill ORGANIC/LEGACY + 'Switch to …' link (window.confirm, PUT /mode :575-591); engine pill RUNNING/STOPPED/NOT REPORTING + '· round #id' + 'heartbeat N s ago' (from GET /status engine.heartbeatAgeMs<20 s); 'saved vN' + 'engine applied vN' (warn colour when behind). Sub-nav `.organic-subnav` Settings / Live round / Round audits (:601-611). SettingsView (:202-261): intro paragraph; one `.rotation-card.organic-group` per group (engine 2 fields, roundStart 9, momentum 12, tiers 14 = 3 tiers×4 + jitter + ladder, asymmetry 5, defenses 11, audit 1) with `.organic-grid` (auto-fill 260px) of FieldRow (:171-200): head = `.rotation-label` + `.organic-applies-{live|next_round}` tag ('applies instantly'/'from the next round') + 'unsaved' badge; FieldInput (:114-169) = `.organic-toggle` (boolean), `.rotation-input.organic-select` (select), text, or number in `.rotation-input-container` with unit suffix; `.organic-help`; foot = 'min · max' + 'default: X' reset link + inline error. Footer `.rotation-footer.organic-footer`: 'N unsaved changes · last saved …', 'Reset to directive defaults' (POST /settings/reset, confirm), 'Discard changes', 'Save & push to engine' (PUT /settings {values} full doc; 400 'path: message' lines mapped to fields via fieldErrorsFrom). LiveView (:274-396): round card (title 'Round #id' + phase text LOCKED/steering/quiet/boost/organic; 12 `Stat` tiles: elapsed/remaining, peak, settings v, lead target, initial wave, tiers, NPC trades by N NPCs, real trades, sim BUY/SELL, last real trade, steering; `.organic-potbar`), projection card (planned defense, projected winner, real losing/winning volume, sim profit, surplus to real, company net, simulator net, reason; executed injection line), recent NPC trades table (t+, NPC id, tier, side, amount, reason — last 25). RoundsView (:400-462): audit table (winner, real loser pool, commission, sim profit, surplus, company/sim net, final defense, locked) with expandable JSON. Polling: status 5 s, live 1 s (only on that view), rounds 10 s (npcOrganicApi.js; endpoints admin-npc-organic.controller.ts).
MECH: Schema-driven form is generic (all 54 fields rendered identically, no grouping by intent, no previews, no 'what changes now'), which is why each setting is hard to understand.
FIX: Keep the schema/API contract; change presentation: per-group card with rows, computed previews (e.g. arrivals curve from momentum, tier stake ranges, expected initial-wave window, peak-hours evaluation 'peak now: yes/no'), 'applies' as a badge with hover explanation, engine-applied vs saved diff, and a compact live strip pinned at top.

OPEN: [
 "Is 'make legacy correct' meant for running legacy in production again, or for making Rotation/Volume/Pool tabs also govern the organic engine (organic tiers vs A7 volume shaping overlap)? The answer decides whether BUG-3/BUG-6 are fixed in the simulator or replaced by organic equivalents.",
 "Should NPC fills appear on the PWA as a public trade tape (BUG-7)? It changes the coalescer contract and exposes NPC identities/avatars to players.",
 "What should 'participation session' mean for rotation caps (per round vs per trade)? Current code and admin copy disagree.",
 "Confirm intended default for the ON/OFF flag when Redis is empty (currently ON); the fix in BUG-1 assumes OFF-by-default is acceptable.",
 "Do you want liquidity_* heartbeat keys surfaced in the NPC Behavior tab, or folded into the pace preset?"
]
