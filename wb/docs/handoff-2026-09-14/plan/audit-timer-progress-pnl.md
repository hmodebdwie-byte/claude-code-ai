######## timer-progress-pnl
ARCH: Timer: the engine process runs `setInterval(publishTimerHeartbeat, 1000)` (apps/engine/src/battle/battle-engine.service.ts:680-682) and publishes `{battleId,timeRemaining,timeElapsed,totalDuration,percentage,isFinalize,timestamp,backfilled?,ending?}` on Redis `engine:events:timer` (observed live on this box: exactly 1000 ms apart, timestamps …778774/…779774/…780774). The single API process (PM2 fork, instances:1 — backend/ecosystem.config.js:21-22; it is HTTP + Socket.IO + Redis subscriber in one event loop) gates every engine channel on a Redis leader lease (redis.subscriber.ts:534-556 → ws-broadcaster-leader.service.ts), queues ticks per battle in `handleTimer` (redis.subscriber.ts:~1096-1136), paces them with a `setTimeout` dispatcher at 1000 ms steady / 250 ms catch-up / 1000 ms for `ending` (scheduleNextTimerDispatch, :~1160-1210, setTimeout at :1182), and `forwardTimerNow` (:~1287-1380) does `wsGateway.emitToBattle(battleId,'timerUpdate',{time_remaining,...})` + `entryMultiplierUpdate` and then kicks `tradesService.emitActiveTradeStreamUpdatesForBattle` on EVERY tick (:1366-1377). `emitToBattle` (ws.gateway.ts:372-390) broadcasts to room `battle_{id}` with compression off for timer events. Client: socket `timerUpdate` → useWebSocket.js:915-952 `onTimerUpdate` → `normalizeSocketTimerUpdate` → `isOutOfOrder` timestamp guard (live/utils/realtimeEventGuards.js:19-28) → `isActiveBattlePayload` (:44-50) → `scheduleHotUpdate('timer', …)` (live/scheduler/hotUpdateScheduler.js, 30 writes/s budget, priority price>timer>snapshot>trade) → `matchStore.setTimer` (stores/matchStore.js:122-131, floor-second signature dedupe) → TradingPanelContainer.jsx:24-33 selects `timer` → TradingPanel.jsx (whole 1150-line component re-renders each second) → TradingPanelCooldownSection → hooks/useBattleCountdown.js:37-43, which displays `Math.floor(timer.time_remaining)` verbatim; there is NO local ticking, no end_time/clock-offset derivation (the helper `remainingSecondsFromEndTime` at :23-27 is unused). Pool bar: engine `BattleSnapshotPublisher.publish` (per NPC fill via coalescer + 1 Hz heartbeat, signature-deduped) → API `handleBattleSnapshot` (redis.subscriber.ts:2451-2532, 250 ms per-battle coalesce) → `emitPersonalizedToBattleRoom` (ws.gateway.ts:396-437: fetchSockets + per-user free-tap enrichment + per-socket emit) → client `onBattleSnapshot` → live/services/battleSnapshotApplyService.js:36-46 (push to store when pots changed or 500 ms) → `matchStore.lastLiveSnapshot` → `useVisiblePots` → TradingPanel.jsx:745-754 `sellPercent` → target ref → perpetual rAF (:756-781) writes `--sell-percent` on `.progress-bar-track`; CSS clamp(13%,x,87%) widths (TradingPanel.css:82-119) with the $ labels rendered INSIDE the clamped segments. Estimator: Header.jsx:250-254 mounts components/WinEstimatorSheet.jsx; its "Market Flip" row (LOSING / pips / imbalance / volume / flip profit) is fetched over REST `GET /battle/market-flip` by live/hooks/useMarketFlip.js (250 ms debounce, 1500 ms max-wait) whenever `potSignature` (Math.round of both pots) or `priceSignature` (sign + 2-pip bucket vs latched start_price) changes, then held by a 1500 ms verdict damper before the grid appears; the server side is battle.service.ts:2446-2597 `marketFlip` → shared `estimateFlipProfit` → `buildPayoutInputs` (loads all stakes, NPC trades, and ALL `price_ticks` rows for the battle) → `computePayouts`. The trading panel's "Potential payout" is a separate feed: engine snapshot → API `emitTradingDashboardToBattleParticipants` (500 ms/battle throttle, redis.subscriber.ts:2511-2525) → per-user `getTradingDashboardPayload` (battle.service.ts:2080-2420, price from `priceTick.findFirst`, own `computeLiveProjectedSettlementPayouts`) → `emitToUserCoalesced('tradingDashboard')` 500 ms → client `normalizeTradingDashboard` → TradingPanel.jsx:296-366 (120 ms stability debounce) → "+$X" (always green, 0 when losing).
KEY FILES: frontend/src/hooks/useBattleCountdown.js (Timer display hook; server time_remaining only, no local tick (unused end_time helper at :23-27))
  frontend/src/utils/useWebSocket.js (Main socket hook: onTimerUpdate :915-952, onPriceUpdate :963-1028, onActiveTrades :1030-1046, dashboard :1130-1150, syncNow :364-401, reconnect :1255-1270, health watchdog :1399-1424, isCooldownUiExclusive :513)
  frontend/src/components/TradingPanel.jsx (Pool bar (:745-803, :977-1006), potential payout (:296-366, :934-940, :1117-1124), perpetual rAF (:756-781))
  frontend/src/components/TradingPanel.css (Pool bar layout :74-119, labels :148-157, dot :159-183)
  frontend/src/components/WinEstimatorSheet.jsx (Profit Estimator sheet: potSignature :138-141, startPrice latch :156-174, priceSignature :187-198, verdict damper :235-268, Market Flip render :310-370)
  frontend/src/live/hooks/useMarketFlip.js (Market Flip fetch loop (debounce/max-wait/requestId invalidation :52-98))
  frontend/src/live/hooks/useWinEstimate.js (Scenario cards fetch loop (200 ms debounce, same invalidation pattern :37-74))
  frontend/src/components/ResumeSyncBridge.jsx (sessionStorage persistence of match slice on every change (:80-165))
  frontend/src/live/scheduler/hotUpdateScheduler.js (30/s store-write budget, type priority)
  frontend/src/live/services/socketHealthService.js (2 s stale watchdog → forced reconnect)
  frontend/src/live/telemetry/realtimeTelemetry.js (Perpetual rAF frame monitor (:51-74), started from useWebSocket.js:455)
  frontend/src/components/NewChart.jsx (Chart rAF loop (:1029-1154), subscribes to timer at :614)
  frontend/src/stores/matchStore.js (timer/lastLiveSnapshot setters with UI-signature dedupe (:63-77, :122-131, :320-331))
  backend/apps/engine/src/battle/battle-engine.service.ts (1 Hz heartbeat setInterval :680-682, publishTimerHeartbeat :759-935 (backfill :787-813))
  backend/apps/api/src/redis/redis.subscriber.ts (Leader-gated Redis→WS forwarder: timer queue/pacer (:~1096-1210), forwardTimerNow (:~1287-1380), price 200 ms coalesce (:880-925), handleBattleSnapshot (:2451-2532), balance snapshot 1 s (:~3385-3560), battle.started global timerUpdate (:2655-2667))
  backend/apps/api/src/redis/ws-broadcaster-leader.service.ts (Leader lease TTL 10 s / renew 3 s (:17-23, :76-78, :143-165))
  backend/apps/api/src/ws/ws.gateway.ts (emitToBattle :372-390, emitPersonalizedToBattleRoom :396-437, per-user coalescing :439-505, TIMER_YIELD_MS :62-66)
  backend/apps/api/src/ws/battle.gateway.ts (subscribeToBattle bootstrap :117-300 (no timerUpdate emitted; projection + chat history + top traders + dashboard per subscribe))
  backend/apps/api/src/trades/trades.service.ts (trade:active:stream refresh :4842-5085, projection cache :631-690 (TTL 300 ms keyed on rounded price), recordAuthoritativeBattleTimer :5104-5115)
  backend/apps/api/src/battle/battle.service.ts (marketFlip :2446-2597, getTradingDashboardPayload :2080-2420 (DB price :2153-2158), dashboard emit :2009-2043, per-snapshot top-traders SQL :2957-2994, free-tap enrichment :1337-1383)
  backend/packages/shared/src/payout/battle-payout.live.ts (assembleLiveSettlementContext :138-358 (unbounded priceTick.findMany :299-303), computeLiveProjectedSettlementPayouts :481-543)
  backend/packages/shared/src/payout/battle-payout.estimate.ts (estimateFlipProfit :497-590, buildPayoutInputs :117+)

## T1 [CONFIRMED/high] Timer digits have no local clock: any late/dropped timerUpdate freezes the display
SYMPTOM: (1) timer freezes / stuck, delayed under load
EVIDENCE: frontend/src/hooks/useBattleCountdown.js:30-43: `timerUpdate.time_remaining is the only source of truth for battle timer display. No local ticking/derivation is done here` … `const secondsFromServer = useServerTimer ? Math.max(0, Math.floor(Number(tr))) : null; const secondsLeft = secondsFromServer ?? null;`. The end_time-based helper exists but is unused: :23-27 `export function remainingSecondsFromEndTime(endTime) { … return Math.max(0, Math.floor((ms - Date.now()) / 1000)); }` (no callers in src). Client re-anchoring on reconnect is absent too: battle.gateway.ts:117-300 `subscribeBattle` emits battleSnapshot/balanceEquityAndPnL/chatHistory/topTradersUpdate/dashboard but never a `timerUpdate`, and useWebSocket.js:984-985 deliberately ignores the timer embedded in priceUpdate.
MECH: The number on screen changes only when a `timerUpdate` frame is delivered and applied. Any hiccup in the chain (API event-loop lag delaying the setTimeout pacer, leader-lease loss (T3), socket reconnect, a tick dropped by the timestamp guard (T9) or by `isActiveBattlePayload` while battleIdRef is null (T10)) shows as a frozen countdown; when frames resume, the API pacer drains the FIFO at 250 ms so the user sees the number jump/run fast. Tab backgrounding is not itself the cause (socket delivery continues and the apply path is synchronous), but on foreground `syncNow` forces a full re-subscribe (useWebSocket.js:364-401 → battle.gateway bootstrap, ~7 DB/Redis round-trips incl. a payout projection) and still gets no timer frame from it.
FIX: Keep server frames as the authority but tick locally: in useBattleCountdown (or matchStore.setTimer) store `anchor = {time_remaining, receivedAtMs: Date.now(), serverTs: timer.timestamp}` and render `max(0, time_remaining - floor((now - receivedAtMs)/1000))` from a 1 Hz setInterval (or rAF-driven when visible); on each new frame re-anchor (snap if |delta| >= 1 s, otherwise keep local phase to avoid jitter). Also compute `end_time = serverTs + time_remaining*1000` once per frame so `remainingSecondsFromEndTime` gives the right value after resume/reconnect. Server: have `subscribeBattle` (battle.gateway.ts after :228) emit one `timerUpdate` built from the cached battle meta (`getActiveBattleId` is already awaited) so a reconnect/foreground re-anchors immediately.

## T2 [CONFIRMED/high] Every 1 Hz timer tick (and every 250 ms snapshot) launches heavy Prisma/projection work on the single broadcaster event loop that also paces the timer
SYMPTOM: (1) timer delayed under heavy load; (2) estimator latency (shares Prisma pool)
EVIDENCE: backend/ecosystem.config.js:21-22 `exec_mode: "fork", instances: 1` (API = HTTP + WS + subscriber in one loop). redis.subscriber.ts:1366-1377 in forwardTimerNow: `void this.tradesService.emitActiveTradeStreamUpdatesForBattle(battleId, lastPrice, { timerRemainingSeconds: … })` on every forwarded tick. trades.service.ts:4901-4928 does 5 parallel reads (`battle.findUnique`, buffered stakes, `sumRealPotsByBattle`, `systemSetting.findUnique`, Redis flag), then :5011-5016 `getActiveTradeStreamProjection` whose cache key is `${battleId}|${roundedPrice.toFixed(5)}|display` with TTL 300 ms (:289-295, :640-644) — i.e. recomputed on practically every tick — calling `computeLiveProjectedSettlementPayouts` → battle-payout.live.ts:299-303 `const priceTicks = await prisma.priceTick.findMany({ where: { battleId }, orderBy: { createdAt: 'asc' }, select: { price: true, createdAt: true } });` (unbounded; one row per priced trade is persisted — redis.subscriber.ts:938-957 `handleTradePriced … { force: true }`), plus `rpcUser.findMany` (:230-233) and `computePayouts` over all price points per winning stake. The same projection is recomputed independently in: `getTradingDashboardPayload` per participant every 500 ms (battle.service.ts:2240-2252, throttle redis.subscriber.ts:2511-2525), `buildSnapshotProjectionCache` every ≥1 s (trades.service.ts:3464-3480, BALANCE_SNAPSHOT_MIN_INTERVAL_MS default 1000 at redis.subscriber.ts:112-114 — the comment above it still says 250 ms), and for every REST `market-flip`/`estimate-payout` call. On top, every 250 ms battleSnapshot flush runs `emitPersonalizedToBattleRoom` (ws.gateway.ts:400-437): `await this.server.in(room).fetchSockets()` then per socket `await personalize(...)` (battle.service.ts:1353-1383 → :1337-1348 `getFreeTapsPerUser()` + Redis GET per user) and `remote.emit(event, personalized)` — with the Redis adapter (redis-io.adapter.ts:29-33, :58-60) each RemoteSocket emit is a separate encode + Redis PUBLISH — and then `.finally(() => emitReadModelSyncAfterSnapshot)` (redis.subscriber.ts:~2575-2585) → battle.service.ts:2986-2994 `emitTopTradersUpdate(battleId, { force: true })` which bypasses its throttle (:2963-2969 `if (!opts?.force && …) return;`) and runs the leaderboard raw SQL every 250 ms. The timer forward itself is a `setTimeout` (redis.subscriber.ts:1182) and therefore inherits all of this loop's lag.
MECH: Under load (many sockets, NPC flow producing a snapshot every 250 ms and thousands of price_ticks rows per round), the broadcaster's loop is saturated by JSON encoding per socket, Redis publishes per socket, Prisma result materialization of full-battle price_ticks several times per second, and per-user projections. The timer dispatcher's setTimeout fires late → wire gaps >1 s → queue depth >1 → catch-up mode at 250 ms → the FE shows freeze-then-jump. Prisma pool contention (local DATABASE_URL has no connection_limit → Prisma default; code comments assume 5) adds latency to the REST estimator calls (E1) and to `balanceEquityAndPnL`.
FIX: (a) Remove the per-tick trade-stream refresh from forwardTimerNow; drive `trade:active:stream` from price/trade changes only, and make the projection cache key coarse (rounded price bucket + pots) with TTL ≥ 1 s. (b) Compute ONE `computeLiveProjectedSettlementPayouts` per (battle, price bucket) per second in the API and share it across dashboard, trade-stream, balance-snapshot and the market-flip/estimate REST handlers (e.g. a small in-memory memo keyed by battleId|priceBucket|potSig with 500-1000 ms TTL). (c) Bound `priceTick.findMany` (downsample server-side, or keep a per-battle in-memory ring of price points fed by the price.tick subscriber). (d) Stop personalizing battleSnapshot per socket every 250 ms: broadcast the shared snapshot once to the room and send `free_taps_left` as a separate per-user event only when it changes. (e) Drop the `force:true` top-traders SQL from the snapshot flush (respect `topTraderEmitThrottleMs`). (f) Longer term: move WS broadcasting to its own process (apps/api/src/ws-service exists) so HTTP/REST projections cannot delay timer emits.

## T3 [HYPOTHESIS/medium] Broadcaster leader lease can silently drop every timer/price/snapshot forward for up to ~13 s after an event-loop stall
SYMPTOM: (1) timer stuck for several seconds then jumps
EVIDENCE: ws-broadcaster-leader.service.ts:17-23 `LEADER_TTL_SEC … '10'`, `LEADER_RENEW_MS … '3000'`; :76-78 `this.renewTimer = setInterval(() => { … }, LEADER_RENEW_MS)`; :146-165 on renew failure `this.isLeader = false; this.logger.warn('WS broadcaster leader lost — renewing failed'); await this.tryAcquire();`. redis.subscriber.ts:543-549: `if (skipRedisParseOnNonBroadcaster() && ENGINE_WS_BROADCAST_CHANNELS.has(channel) && !this.shouldBroadcast()) { return; }` (default true, api-load-profile.env.ts:44-49).
MECH: If the single API process's loop stalls >10 s (or Redis renew errors), the key expires, `isLeader` flips false, and all engine channels are discarded unparsed until the next 3 s tick re-acquires — a total blackout of timer/price/snapshot with no client-side fallback (T1). The engine's backfill then arrives as a burst that the pacer drains at 250 ms.
FIX: Renew from a lightweight path (unref'd interval is fine) but treat a lost lease on a single-instance deployment as non-fatal: keep forwarding when `PUBSUB NUMSUB` shows one subscriber / when `instances:1`, and log loudly. Check production API logs for 'WS broadcaster leader lost' to confirm or rule out.

## E1 [CONFIRMED/high] Market Flip fetch loop invalidates in-flight responses on every pot tick and holds new verdicts 1.5 s — under NPC flow the LOSING state can take seconds or never update
SYMPTOM: (2) estimator takes seconds to realise the user is losing / show flip numbers
EVIDENCE: frontend/src/live/hooks/useMarketFlip.js:52-98: `const requestId = ++requestIdRef.current;` runs on EVERY effect re-run (deps `[battleId, potSignature, priceSignature, enabled]`), and the response is discarded if anything changed since: :72 `.then((res) => { if (requestId !== requestIdRef.current) return; setData(res ?? null);`; each dispatch also aborts the previous request :65 `abortRef.current?.abort();`; the 250 ms trailing debounce (:96 `setTimeout(dispatch, MARKET_FLIP_DEBOUNCE_MS)`) is restarted by every re-run and only the 1500 ms ceiling (:91-94) forces a dispatch. The trigger rate is per-dollar: WinEstimatorSheet.jsx:138-141 `potSignature = \`${Math.round(Number(buyPot) || 0)}:${Math.round(Number(sellPot) || 0)}\``; pots reach the store on every forwarded snapshot whose pots changed (battleSnapshotApplyService.js:36-46 `potsChanged || now - last >= SNAPSHOT_THROTTLE_MS`; server gap 250 ms). DB shows NPC-dominated pots (battle 81: buyPot 203,695 vs sellPot 3,160) so pots move on essentially every snapshot. After a LOSING response lands, WinEstimatorSheet.jsx:235-268 holds it: :20 `VERDICT_DAMP_MS = 1500` … :261 `const timer = setTimeout(() => { … setShownVerdict(...) }, VERDICT_DAMP_MS)`. Server: battle.service.ts:2446-2597 `marketFlip` awaits battle row, Redis stakes, `resolveLiveCurrentPrice` (:315-332), pots, NPC flag, then :2527 `estimateFlipProfit` → battle-payout.estimate.ts:117+ `buildPayoutInputs` (stakes, NPC trades, battle, fee, rpcUser, full `priceTick.findMany`) + `computePayouts`.
MECH: Chain from a price crossing: engine price.tick → API 200 ms coalesce (redis.subscriber.ts:218-220, :880-925) → client 50 ms throttle (pricePipeline.js:24-37) → `priceSignature` sign flips → effect re-run. From then on, with pots ticking every ~250-500 ms, the debounce never matures, a request goes out only at the 1500 ms ceiling, and its response is accepted only if it returns before the NEXT pot tick (~250 ms). Best case ≈ 200+50+RTT+1500 ≈ 2-2.5 s before the grid appears; with REST latency above the inter-tick gap (Prisma pool shared with T2's hot paths) every response is thrown away and the row keeps showing the previous verdict or 'Updating…' indefinitely — worst case zero accepted responses, invisible in rate-limit metrics. The same requestId pattern exists in useWinEstimate.js:48-57 (scenario cards) with a 200 ms debounce and no ceiling at all.
FIX: In useMarketFlip: (1) keep `requestId` per DISPATCH, not per effect run — increment it inside `dispatch()` and only abort/discard when a newer request has actually been sent; (2) accept the latest completed response and use a leading+trailing throttle (e.g. fetch at most every 750 ms, always fire immediately on a `priceSignature` SIGN change since that is the verdict flip) instead of a starvable trailing debounce; (3) quantize `potSignature` (e.g. 0.5 % of total pot) so NPC churn doesn't retrigger; (4) drop or shorten `VERDICT_DAMP_MS` to ~300 ms once the server adds a pip deadband (the damper only exists because battle.service.ts:2503-2515 uses a bare sign test). Better still: push the verdict over the socket — the server already computes the user's projected side and payout per tick in `trade:active:stream`/`equityAndPnL` (`projected_winner_side`, redis.subscriber.ts:~1382-1400); add `net_side`, `distance_pips`, `volume_required` and `flip_profit` to that per-user frame (computed from the shared projection in T2) and render Market Flip from the store with no REST round-trip.

## E2 [CONFIRMED/medium] Potential payout uses a DB-lagged price and a different projection than Market Flip, and cannot show 'losing' (dead PnL code, always-green colour)
SYMPTOM: (2) panel realises losing late / disagrees with estimator
EVIDENCE: battle.service.ts:2153-2158 `const priceTick = await this.prisma.priceTick.findFirst({ where: { battleId }, orderBy: { id: 'desc' }, select: { price: true } }); const currentPrice = priceTick ? Number(priceTick.price) : startPrice;` (price_ticks are batch-flushed every 150 ms, redis.subscriber.ts:97-99, and only trade-attributed/heartbeat ticks are persisted) whereas `marketFlip` uses Redis `ticktrade:price:last` (:319-321). Losing users get :2367-2400 `payoutIfTeamWins = projectedWinnerSide === 'BUY' ? … : 0` and the client renders TradingPanel.jsx:1120-1122 `<span style={{ color: potentialPayoutColor }}>+${potentialPayoutForDisplay.toFixed(2)}` with :937-940 `potentialPayoutColor = potentialPayoutForDisplay >= 0 ? '#3DB44C' : '#d51717'` after :936 `Math.max(0, displayedPotentialPayout)` — never red. :934 `const displayPnlForUI = Math.max(0, displayPnl ?? 0);` is computed but never rendered. Update cadence: engine snapshot → 500 ms battle throttle (redis.subscriber.ts:2511-2525) → per-user signature dedupe (battle.service.ts:1975-2027) → 500 ms `emitToUserCoalesced('tradingDashboard')` (ws.gateway.ts:453-459) → 120 ms `PAYOUT_STABLE_MS` (TradingPanel.jsx:228, :356-359).
MECH: The dashboard and Market Flip can evaluate different prices for the same instant (DB tail vs Redis last), so the panel may show +$X while the sheet says LOSING (or vice versa). The panel's only 'losing' signal is +$0.00, delivered ~1-1.6 s after the snapshot at best, and hidden behind the 900 ms `ZERO_COMMIT_MS` path for pnl. The trade-stream frame that already carries the authoritative countdown is not used for this display.
FIX: Use `resolveLiveCurrentPrice`/`lastKnownPriceByBattle` in getTradingDashboardPayload; render a signed/losing state (side badge + red when `projected_winner_side !== user side`) from `equityAndPnL.projected_winner_side` / `display_pnl` which already arrive per user; delete the dead `displayPnlForUI`; feed both panel and estimator from the single shared projection (T2 fix b).

## B1 [CONFIRMED/medium] Pool bar labels live inside the width-clamped segments, so the small pool's number is overlapped by the dot and the big segment
SYMPTOM: (3) progress bar hides the smaller pool's number when one side is huge
EVIDENCE: TradingPanel.jsx:983-1004: the `$` label is a child of each segment: `<div className={\`progress-sell …\`} style={PROGRESS_SELL_STYLE}><span className="progress-label">…{formattedSellPot}</span></div>` … `<div className="progress-indicator-dot" …/>` … `<div className={\`progress-buy …\`} style={PROGRESS_BUY_STYLE}>…{formattedBuyPot}`. Widths: :61-62 `PROGRESS_SELL_STYLE = { width: 'clamp(var(--min-pos), var(--sell-percent), var(--max-pos))' }`, buy = `calc(100% - …)`; TradingPanel.css:88-89 `--min-pos: 13%; --max-pos: 87%;`, :93-101 `.progress-sell { … padding-left: 12px; … overflow: visible; }`, :109-119 `.progress-buy { … justify-content: flex-end; padding-right: 12px; overflow: visible; }`, :148-157 `.progress-label { display: inline-block; white-space: nowrap; … font-size: 15px; }` (no z-index/position), :159-183 `.progress-indicator-dot { position: absolute; left: clamp(var(--min-pos), var(--sell-percent), var(--max-pos)); … transform: translateX(-50%); width: 22px; height: 22px; z-index: 9; background: #fff; }`.
MECH: At the 13 % floor on a ~360-400 px bar the segment is ~47-52 px wide minus 12 px padding ≈ 35-40 px, but a bold 15 px label like `$3.16K`/`$203.7K` is 55-65 px, so it overflows past the segment edge exactly where the 22 px white dot (z-index 9) is centred; the dot hides the label's tail and the remainder lands on the other side's gradient. Same on the buy side at 87 %. `FriendProfileModal` receives the same `sellPercent/formatted*` (TradingPanel.jsx:1142-1147) and inherits the problem if it reuses the CSS.
FIX: Decouple labels from the fills: render both labels as absolutely positioned children of `.progress-bar-track` anchored to the track edges, above the dot — e.g. `.progress-label{position:absolute;top:0;bottom:0;display:flex;align-items:center;z-index:10;pointer-events:none;text-shadow:0 1px 2px rgba(0,0,0,.6)} .progress-label.sell{left:12px} .progress-label.buy{right:12px}` and move the two `<span>`s out of `.progress-sell/.progress-buy` (keep those as pure colour fills). Then `--min-pos/--max-pos` only govern the fill and can stay at 13/87 %, or be set from a measured label width (`labelWidth + 11px dot radius`) via ResizeObserver if you want the dot never to sit on a label. Optional: shorten `formatAmount` to one decimal for ≥100K (`$203.7K` → `$204K`) to bound label width.

## P1 [CONFIRMED/medium] Perpetual 60 fps rAF loops run for the whole round even when nothing changes (pool-bar smoothing, telemetry frame monitor, chart)
SYMPTOM: (1) freezes/jank on mobile
EVIDENCE: TradingPanel.jsx:756-781: `const tick = () => { … track?.style.setProperty('--sell-percent', \`${s}%\`); progressRafIdRef.current = requestAnimationFrame(tick); }; progressRafIdRef.current = requestAnimationFrame(tick);` — unconditional reschedule; writing the custom property invalidates the two `clamp()` widths and the dot's `left` every frame. realtimeTelemetry.js:51-74 `startDroppedFrameMonitor` … `rafId = requestAnimationFrame(loop);` (unconditional), started in production at useWebSocket.js:455 `realtimeTelemetry.startDroppedFrameMonitor();`. NewChart.jsx:1029-1154 animate loop reschedules itself every frame (`scheduleAnimate()` at the tail, :1154) and by design moves the expiry line each frame (:1091-1123).
MECH: Three always-on rAF loops plus per-frame style invalidation of the bar keep the compositor/main thread busy on low-end phones and starve React commits triggered by socket events (timer, pots). The bar loop is pure overhead after convergence (`Math.abs(target - s) < EPS` snaps but still reschedules).
FIX: Bar: schedule rAF only while `|target - s| >= EPS` (restart from the `useLayoutEffect` that sets `targetSellPercentRef`), and stop writing when unchanged. Telemetry: gate `startDroppedFrameMonitor` behind `import.meta.env.DEV` or a debug flag. Chart: keep one loop but skip the expiry-line write when x moves < 1 px and pause when the tab is hidden (already implicit for rAF).

## P2 [CONFIRMED/medium] ResumeSyncBridge writes the whole resume snapshot to sessionStorage on every price/timer change (throttle helper is dead code)
SYMPTOM: (1) jank under heavy load on mobile
EVIDENCE: ResumeSyncBridge.jsx:123-128 defines `schedulePersistFromMatchSlice` (750 ms throttle) but it has no callers (`grep schedulePersistFromMatchSlice` → only the definition at :123). The active subscription :130-165 calls `appResumeStore.getState().saveSnapshot({… price: slice.price, timer: slice.timer, topTraders, activeTrades, recentCandles: prices.slice(-50) …})` directly on every shallow change of a slice that includes `price: state.price` (:86) and `timer: state.timer` (:87). appResumeStore.js:82 `saveSnapshot: (payload) => set(...)` under zustand `persist` with `createJSONStorage(() => sessionStorage)` (:104-122) → JSON.stringify of the partialized state + synchronous `sessionStorage.setItem` per set.
MECH: Price writes reach matchStore up to 20×/s (pricePipeline.js PRICE_THROTTLE_MS 50) and timer 1×/s; each triggers a synchronous serialize+write of top traders, up to 200 active trades, 50 candles and the final snapshot — tens of KB, up to ~20 times per second, on the main thread.
FIX: Route the subscription through `schedulePersistFromMatchSlice` (leading-edge 750 ms as intended), drop `price` from the persisted slice (it is re-hydrated from the socket anyway), and/or write via `requestIdleCallback`.

## P3 [CONFIRMED/medium] Client stale-watchdog force-reconnects every client after ~10-12 s without data, amplifying server load exactly when the broadcaster is slow
SYMPTOM: (1) timer stuck then everything reloads; server load spikes
EVIDENCE: useWebSocket.js:1399-1424 `startSocketHealthWatchdog({ … onStale: () => { forceReconnectAndSync(); forceLineChartSync(); }, intervalMs: 2000, staleThresholdMs: 5000 + Math.floor(Math.random() * 1000), requiredConsecutiveStaleChecks: 2, minRecoveryGapMs: 9000 })`; socketHealthService.js:15-48 setInterval + `onStale()`. A reconnect re-runs the full `subscribeBattle` bootstrap (battle.gateway.ts:117-300: `getActiveBattleId`, 2× `battle.find*`, bootstrap snapshot, `engine:commands:battle.join` publish, `user.findUnique` + `getEquityAndPnlForBalanceEvent` (a payout projection), chat history 100 rows, top-traders SQL, dashboard payload).
MECH: When timer/price frames stall for ~10-12 s (T2/T3), every connected client (jitter only 0-1 s) disconnects and reconnects within the same second, each costing ~8 DB/Redis round-trips plus a projection on the same saturated process — a feedback loop.
FIX: Raise the stale threshold and use wider jitter/exponential backoff; on stale, first send a cheap `ping`/`subscribeToPrice` and only reconnect if that also fails; make the subscribe bootstrap lighter (skip chat history/top traders on a `reason:'resync'` subscribe).

## E3 [CONFIRMED/medium] Scenario-card estimate fires a POST /battle/estimate-payout (two projections server-side) on every $1 pot move while the sheet is open
SYMPTOM: (2) estimator load / latency
EVIDENCE: useWinEstimate.js:5 `ESTIMATE_DEBOUNCE_MS = 200`; :37-74 effect deps `[battleId, amount, potSignature, enabled, validationError]` with `const requestId = ++requestIdRef.current;` then `fetchWinEstimate` → winEstimateService.js:30-40 `axiosInstance.post('/battle/estimate-payout', …)`; potSignature granularity is $1 (WinEstimatorSheet.jsx:138-141). Server `estimatePayout` (battle.service.ts:2624+) prices both sides via `computeDualScenarioProjection` (documented in battle-payout.live.ts:104-111).
MECH: With NPC flow the debounce matures roughly every 250-500 ms, so each open sheet costs 2-4 POSTs/s, each running the full stake/NPC/price_ticks load twice; responses arriving after the next pot tick are discarded by the same requestId pattern as E1.
FIX: Quantize potSignature (≥0.5 % of total pot), throttle to ≤1 req/s, fix the requestId invalidation as in E1, and serve estimates from the shared per-battle projection memo (T2 fix b).

## T9 [HYPOTHESIS/low] Timestamp-ordering guard can drop engine timer ticks after the API-stamped battle.started tick when host clocks are skewed
SYMPTOM: (1) timer stuck at round start
EVIDENCE: redis.subscriber.ts:2655-2667 emits `timerUpdate` with `timestamp: now` where `now = Date.now()` of the API host; engine ticks carry the engine host's `timestamp: now` (battle-engine.service.ts:838-848). Client realtimeEventGuards.js:19-28 `if (prev != null && ts < prev) return true;` keyed by event name ('timerUpdate'), cleared only on battle switch (battleSnapshotTransitionService.js:37-40).
MECH: If the API host clock leads the engine host clock by Δ seconds, engine ticks are rejected as out-of-order for Δ seconds after each `newBattleStarted`, freezing the countdown at the start value. Same host locally, so not reproducible here; relevant only for multi-host production.
FIX: Stamp the synthesized tick with the engine's `payload.timestamp` (available from battle.started), or compare on a monotonic per-battle sequence instead of wall clock; at minimum tolerate ≤2 s of backwards skew for 'timerUpdate'.

## T10 [CONFIRMED/low] Timer ticks are discarded while the client has no battle id (after cooldown reset) until a snapshot arrives
SYMPTOM: (1) blank/frozen timer for the first second(s) of a round
EVIDENCE: realtimeEventGuards.js:44-50 `if (activeBattleId == null) { return payloadBattleId == null; }` and useWebSocket.js:921-922 `if (!isActiveBattlePayload(payloadBattleId, battleIdRef.current)) return;`; `battleIdRef` is only set from battleSnapshot/marketSnapshot/newBattleStarted (battleSnapshotTransitionService.js:30-31, useWebSocket.js:879-910), and cooldown end resets battleId to null (timerCooldownTransitionService.js:247-265).
MECH: Every engine tick carries `battleId`, so with `battleIdRef.current == null` the guard returns false and the tick is dropped; the digits stay at '00:00' until the first snapshot/newBattleStarted for the new round is applied.
FIX: When `activeBattleId == null` accept the tick and adopt its battle_id (or set battleIdRef from `timerUpdate.battle_id` when the store has none).

OPEN: [
 "Production topology: are engine and API on the same host (clock skew for T9) and does the API run with the same `instances:1` fork as ecosystem.config.js? Do production API logs contain 'WS broadcaster leader lost' or 'PrismaSlowQuery'/P2024 during the reported freezes (T2/T3)?",
 "Production DATABASE_URL `connection_limit`: local env has none (Prisma default), while trades.service/redis.subscriber comments assume 5 — which value is deployed, and how many `price_ticks` rows does a busy production round accumulate (drives the cost of every projection)?",
 "How many sockets does the broadcaster serve at peak? The per-socket personalized battleSnapshot emit (T2) scales linearly with sockets at 4 Hz; a socket-count figure would let the fix be prioritized.",
 "Frontend timers inventory during a round (for reference): TradingPanel rAF 60 fps (:756-781, perpetual); realtimeTelemetry rAF 60 fps (perpetual, prod); NewChart rAF 60 fps while chart has data; chartRafScheduler rAF only when a tick is pending (≤60 fps); hotUpdateScheduler setTimeout only when over the 30 writes/s budget; pricePipeline 50 ms throttle (≤20 store writes/s); tradePipeline 100 ms batch setTimeout; socketHealthService setInterval 2 s; ResumeSyncBridge persist on every slice change (P2) + lineChartStore 400 ms trailing debounce (starves under continuous ticks) + DEV-only 100 ms probe; TradingPanel setTimeouts 120 ms (payout/equity stability), 900 ms (zero commit), 1000 ms (glow), 600 ms (particles), 2000 ms idle → REST wallet refetch; WinEstimatorSheet 200 ms / 250 ms+1500 ms / 1500 ms damper; Notifications 15-min poll; StatusMessage 2 s; Toast timers; useKeyboard 100 ms; per-second React re-renders from `matchStore.timer`: TradingPanel (whole component via TradingPanelContainer.jsx:24-33), IndicatorsContainer/IndicatorsPanel (:12-19), NewChart (:614), ResumeSyncBridge slice — consider selecting only `time_remaining`/`is_finalize` where the full object is not needed."
]
